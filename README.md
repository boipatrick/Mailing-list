M.U.A.S is a script that automates the unsubscribing from bulk mailing lists. 

Backend -- Django

To setup django, run on terminal: pip install django

To debug live page, run: python manage.py runserver

